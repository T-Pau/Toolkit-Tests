[settings]
program-directories = @TEST_DIRECTORY@/scripts
    @TOOLKIT_DIRECTORY@/bin
test-input-directories = @TEST_DIRECTORY@
    @TEST_DIRECTORY@/data

[environment]
TOOLKIT_DIRECTORY = @TOOLKIT_DIRECTORY_FROM_INSIDE_SANDBOX@

[suite]
tests = *.test
# expected-failing-tests

[comparator-preprocessors]
s. = preprocess-s

[comparators]
zip.zip = zipcmp -v
